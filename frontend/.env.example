# Backend API base (leave as /api to use the dev proxy in vite.config.ts)
VITE_API_URL=/api

# Portal URL to redirect to when there's no valid session (Portal is now
# the only place that handles login).
VITE_PORTAL_URL=http://localhost:5174
